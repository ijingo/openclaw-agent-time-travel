# TODO

- Support listing the currently tracked markdown files from inside OpenClaw.
- Support user-configured extra tracked markdown paths.
- Support removing or disabling tracked paths after they were added.
- Improve tag delivery for threaded channels where outbound hooks do not expose `sessionKey`.

